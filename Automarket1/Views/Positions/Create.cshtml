@model Automarket1.Position

@{
    ViewData["Title"] = "Створити посаду";
}

<h1>Створити посаду</h1>

<h4>Посади</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Position1" class="control-label"></label>
                <input asp-for="Position1" class="form-control" />
                <span asp-validation-for="Position1" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Створити" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Повернутися до списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
